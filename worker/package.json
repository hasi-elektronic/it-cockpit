{
  "name": "it-cockpit-api",
  "version": "0.1.0",
  "description": "Hasi IT-Cockpit Worker API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute it-cockpit --file=../db/schema.sql --remote",
    "db:seed-admin": "wrangler d1 execute it-cockpit --file=../db/seed-admin.sql --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.30.0"
  }
}
